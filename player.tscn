[gd_scene load_steps=4 format=3 uid="uid://dghlvllfqxfl0"]

[ext_resource type="Texture2D" uid="uid://brd0h4a0v54rv" path="res://icon.svg" id="1_vettu"]

[sub_resource type="GDScript" id="GDScript_0gctd"]
script/source = "extends CharacterBody2D

@export var acceleration : int = -10

const SPEED = 800.0
const JUMP_VELOCITY = -1400.0

# Get the gravity from the project settings to be synced with RigidBody nodes.
var gravity = 2500


func _physics_process(delta):
	# Add the gravity.
	if not is_on_floor():
		velocity.y += gravity * delta

	# Handle Jump.
	if Input.is_action_just_pressed(\"ui_accept\") and is_on_floor():
		velocity.y = JUMP_VELOCITY

	# Get the input direction and handle the movement/deceleration.
	# As good practice, you should replace UI actions with custom gameplay actions.
	var direction = Input.get_axis(\"walk_left\", \"walk_right\")
	if direction:
		velocity.x = direction * SPEED
	else:
		velocity.x = move_toward(velocity.x, 0, SPEED)
		
	if direction == 0:
		velocity.x = move_toward(velocity.x, 0, acceleration)
	else:
		velocity.x = move_toward(velocity.x, SPEED * direction, acceleration)

	move_and_slide()
"

[sub_resource type="RectangleShape2D" id="RectangleShape2D_vr8bo"]
size = Vector2(120, 123)

[node name="player" type="CharacterBody2D"]
script = SubResource("GDScript_0gctd")

[node name="CollisionShape2D" type="CollisionShape2D" parent="."]
position = Vector2(594, 310.5)
shape = SubResource("RectangleShape2D_vr8bo")

[node name="Sprite2D" type="Sprite2D" parent="."]
position = Vector2(594, 312)
texture = ExtResource("1_vettu")
